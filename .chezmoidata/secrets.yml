# Secret data is stored separately, but still merged under `user`.
user:
  git:
    email: 'cccc@example.com'
    name: 'dddd'
  services:
    aria2:
      secret: 'supersecret'
  profile:
    public:
      email: 'cccc@example.com'
      git_username: 'dddd'
    private:
      email: 'kkkk@example.com'
